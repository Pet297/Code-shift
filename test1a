function Add(a, b)
{ 
    var c = 0;
    c = a + b;
    return c;
}
const input = "function Add(a, b)\n{\nvar c = 0;\nc = a + b;\nreturn c;\n}";
let k = Add(100,200);
let l = Add(k,200);
;
class C2 extends C1 {
    constructor() {
		super();
	}

	enterProgram(ctx) {
	}

	enterFunctionDecl(ctx) {
		this.l.push(ctx.getText());
	}
}
const instance = new C2();
k = k + l;
l = Add(Add(200,300) - 1000 * k, l / k) << 2;
if (10 > 20)
{
    let m = 100;
    k += m + m;
}
else if (20 > k)
{
    ;
}
else
{
    const n = 200 + k;
    k **= 2;
}
do ; while (50 == 40);
while (4000) { { { let m = 250; } } }
for (k = k + l; True; k /= 50, l /= 60)
{
    function Add(a, b)
    { 
        var c = 0;
        c = a + b;
        return c;
    }
    l += Add(l + l, l);
}
//with (c = 1 + 1) Console.Log(c);
labeled: labeled2: labeled3: l += l;
/*switch (l)
{
    case 0:
}*/
