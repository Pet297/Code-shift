var a = 0;
var b = 1;
var c = [1,2,3];

do { a = a + b; b = b + a; } while (a > b);
while (a + b == 1) { b += a; b += a; }
for ( a = 0; a < 20; a++) { a += 3; b += 3; }
for ( var i = 0; i < 20; i++) { b += 3; b += 4; }
for ( var key in c ) { a += 2; a += 3; a += 4; }
for ( var item of c ) { a += item * 2; b += item; }
